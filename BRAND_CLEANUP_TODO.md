# BRAND CLEANUP TODO

本文件仅记录本轮暂不修改的品牌清理延后项，避免和已经处理完成的外显项混在一起。

## 暂不修改的内部命名

### `hemaphp` 配置键与内部开关
- 当前项：`app.hemaphp.*` 配置键、内部版本号字段、远程开关字段。
- 所在范围或文件：`TP8服务端/config/app.php` 及所有 `config('app.hemaphp...')` 调用点。
- 暂不修改原因：这类名称已经深入配置读取链路，强改会扩大回归面。
- 后续建议处理方式：后续统一设计一套新的内部配置命名，再配合全局替换和回归测试处理。

### `hema` / `Hemaphp` 内部类名与缓存标识
- 当前项：`extend/hema/*`、`app/store/controller/Hemaphp.php`、缓存键 `hemaphp`。
- 所在范围或文件：`TP8服务端/extend/hema`、`TP8服务端/app/store/controller`、相关控制器和服务调用链。
- 暂不修改原因：当前主要承担内部路由、服务封装和缓存读写，直接改名会牵动较多引用关系。
- 后续建议处理方式：后续按模块拆分重命名，先补自动化回归点，再逐步替换类名、命名空间和缓存键。

### 启动引导中的上游包命名
- 当前项：`vendor/hemaphp/think-addons` 的加载路径与自动加载入口。
- 所在范围或文件：`TP8服务端/think`、`TP8服务端/public/index.php`。
- 暂不修改原因：这部分属于依赖接入层，当前不影响对外展示，先保持稳定。
- 后续建议处理方式：后续如果替换插件体系或自行维护分叉包，再统一调整包名和加载路径。

### 业务代码中的历史注释与技术标识
- 当前项：配送、任务、订单等代码里的历史注释和技术说明文字。
- 所在范围或文件：如 `TP8服务端/app/api/controller/Delivery.php`、`TP8服务端/app/common/model/Order.php` 等业务代码。
- 暂不修改原因：这些内容主要面向开发维护，不直接出现在业务界面。
- 后续建议处理方式：后续结合模块重构或编码规范清理时统一中性化。

## 暂不修改的依赖与上游元数据

### Composer 依赖元数据
- 当前项：包名 `hemaphp/think-addons`、作者邮箱、分发地址等元数据。
- 所在范围或文件：`TP8服务端/composer.lock`、`TP8服务端/vendor/composer/installed.json`、`TP8服务端/composer.json` 的依赖声明。
- 暂不修改原因：这些内容属于上游依赖元数据，直接改会影响依赖一致性和后续安装。
- 后续建议处理方式：后续若确定长期维护分叉版本，再通过私有包或本地路径仓库替换上游依赖。

### 第三方许可证与作者声明
- 当前项：许可证、作者署名、依赖包自带说明。
- 所在范围或文件：`LICENSE`、`LICENSE.txt`、`vendor` 内各类许可证和说明文件。
- 暂不修改原因：这类内容涉及第三方合规信息，不适合机械替换。
- 后续建议处理方式：后续按合规要求保留上游声明，并在你自己的项目说明里单独补充二开声明。

## 后续可选优化项

### 历史更新说明与隐藏说明文件
- 当前项：历史更新记录里的旧品牌名称，例如隐藏的 `.txt` 说明文件。
- 所在范围或文件：`TP8服务端/.txt`、`TP8服务端/addons/single/.txt` 等历史说明文本。
- 暂不修改原因：这类文件不直接参与当前业务流程，收益低于修改风险。
- 后续建议处理方式：后续整理文档体系时按“历史归档”或“中性化重写”方式统一处理。

### 小程序打包产物中的品牌残留
- 当前项：已打包输出中的旧品牌字样、旧资源引用和压缩后的内部标识。
- 所在范围或文件：`微信小程序/mp-weixin/common`、`微信小程序/unpackage` 等构建产物目录。
- 暂不修改原因：直接手改产物难以维护，后续重新构建时也容易被覆盖。
- 后续建议处理方式：优先回到源配置和源代码处理，再通过重新打包覆盖产物。

### 进一步中性化的文案细节
- 当前项：后台局部提示语、默认描述文案、部分非核心帮助文本。
- 所在范围或文件：后台模板、安装页剩余说明文字、插件说明页等。
- 暂不修改原因：当前不影响跳转、资源加载和核心品牌暴露，优先级低于功能稳定性。
- 后续建议处理方式：后续结合你的正式品牌方案，按页面清单逐步替换为统一文案。
